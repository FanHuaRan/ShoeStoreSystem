<UserControl x:Class="ShoeStoreMvvm.Views.ShoeItemMananageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="1000"
             Background="{StaticResource BackgroundBrush}"
             DataContext="{Binding Source={StaticResource Locator}, Path=ShoeItemMananageViewModel }">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="200"></ColumnDefinition>
            <ColumnDefinition Width="4.5*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="5*"/>
                <RowDefinition Height="35*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <ListView Grid.Row="1" Margin="0" 
                      ItemsSource="{Binding ShoeItems}"
                      BorderThickness="0"
                      Background="Transparent"
                       SnapsToDevicePixels="True"                     
                       SelectionMode="Single" >
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="鞋编号" DisplayMemberBinding="{Binding Path=ShoeItemId}" Width="75"/>
                        <GridViewColumn Header="货号" DisplayMemberBinding="{Binding Path=Shoe.ThingId}" Width="75"/>
                        <GridViewColumn Header="颜色" DisplayMemberBinding="{Binding Path=Color}" Width="75"/>
                        <GridViewColumn Header="尺码" DisplayMemberBinding="{Binding Path=Size}" Width="75"/>
                        <GridViewColumn Header="适用性别" DisplayMemberBinding="{Binding [Season.SeasonName]}" Width="75"/>
                        <GridViewColumn Header="季节" DisplayMemberBinding="{Binding ShoeItemId}" Width="75"/>
                        <GridViewColumn Header="详细" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Tag="{Binding }" HorizontalAlignment="Center" Style="{StaticResource NewDialogBtStyleG}"></Button>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="删除" Width="40">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Tag="{Binding }" Height="30" Width="30" Background="Tomato" Content="x" Foreground="White">
                                        <Button.Clip>
                                            <EllipseGeometry Center="15,15" RadiusX="8" RadiusY="8"></EllipseGeometry>
                                        </Button.Clip>
                                    </Button>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
